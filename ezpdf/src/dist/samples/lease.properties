lease.defaultValue=_________________________
day=${lease.defaultValue}
month=${lease.defaultValue}
year=${lease.defaultValue}
landlordName=${lease.defaultValue}
tenantName=${lease.defaultValue}
checkedLeasingAgent=__
leasingAgentName=${lease.defaultValue}
leasingAgentAddress=${lease.defaultValue}
checkedNotLeadBasedAct=__
checkedLeadBasedAct=__
rentalAddress=${lease.defaultValue}
totalMonths=${lease.defaultValue}
leaseStartDate=${lease.defaultValue}
leaseEndDate=${lease.defaultValue}
totalRent=${lease.defaultValue}
monthlyRent=${lease.defaultValue}
methodOfPayment=${lease.defaultValue}
maxOccupancy=${lease.defaultValue}
securityDeposit=${lease.defaultValue}
lateFee=${lease.defaultValue}
checkedAllowPets=__
allowedPets=${lease.defaultValue}
signatureDate=${lease.defaultValue}
signatureLandlord=${lease.defaultValue}
signatureTenant=${lease.defaultValue}
